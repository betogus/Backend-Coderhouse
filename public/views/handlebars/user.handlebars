<div class="user-container">
    <h1 id="title">{{username}}</h1>

    <div>
        <div class="img-container">
            <div id="pen-container">
                <i class="fa-solid fa-pen"></i>
            </div>
            {{#if photo}}
            <img src="/database/uploads/{{photo}}" />
            {{else}}
            <img src="/database/predeterminados/avatar.jpg" />
            {{/if}}
        </div>
        <p>Email: {{email}}</p>
        <p>Dirección: {{address}}</p>
        <p>Edad: {{age}}</p>
        <p>teléfono: {{#if phone}} +54 351{{phone}} {{else}} No hay datos {{/if}}</p>

    </div>
</div>
    
<style>
    #title {
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        width: 100%;
    }

    #pen-container {
        background-color: rgb(26, 26, 47);
        position: relative;
        left: 100px;
        width: 18px;
        height: 18px;
        padding: 5px;
        border-radius: 50%;
        color: white;
        cursor: pointer;
    }

    .user-container {
        background-color: rgb(32, 32, 60);
        padding-top: 100px;
        border-radius: 2rem;
        width: 80%;
        display: flex;
        flex-direction: column;
        align-items: center;
        color: #ffffff;
        width: 400px;
        margin: 10px;
    }

    .item-name {
        font-size: large;
        margin: 0;
    }

    .item-precioKg {
        font-size: medium;
        margin: 0;
        color: #656565;
    }

    h1 {
        color: #ffffff;
    }

    img {
        width: 100px;
        height: 100px;
        border-radius: 50%;
    }

    .img-container {
        display: flex;
        justify-content: center;
    }
</style>