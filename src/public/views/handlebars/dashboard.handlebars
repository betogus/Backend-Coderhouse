<div class="container-item">
    <div id="title">
        <h1>Bienvenido {{user.username}}</h1>
        <div id="logoutBtn" onclick="location.href = '/auth/logout'">Cerrar Sesión</div>
    </div>
    <h1 id="titulo">Vista de Productos</h1>


    {{#if products.length}}
    <table>
        <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Foto</th>
            {{#each products}}
        </tr>

        <tr>
            <td>{{title}}</td>
            <td>{{price}}</td>
            <td><img src="/uploads/{{thumbnail}}" class="foto" /></td>
        </tr>
        {{/each}}
    </table>
    {{else}}
    <h2> No hay productos por mostrar </h2>
    {{/if}}
</div>

<script src="/socket.io/socket.io.js"></script>
<style>
    #title {
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        width: 100%;
    }

    table {
        background-color: #1f1e1e;
        color: #ffffff;
        width: 400px;
        padding: 10px;
        text-align: center;

    }

    h1 {
        color: #ffffff;
    }


    .container-item {
        margin-top: 100px;
        background-color: rgb(32, 32, 60);
        border-radius: 2rem;
        width: 600px;
        padding: 50px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .foto {
        object-fit: contain;
        max-width: 30px;
    }

    #button,
    #logoutBtn {
        background-color: rgb(101, 65, 245);
        padding: 0px 10px;
        width: 100px;
        height: 30px;
        border-radius: 0.5rem;
        margin: 5px;
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: pointer;
    }

    a {
        text-decoration: none;
        color: white;
    }

    .email {
        color: blue;
    }

    .date {
        color: red;
    }

    .message {
        color: green;
        font-style: italic;

    }
</style>